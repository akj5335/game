version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: neonplay_db
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:alpine
    container_name: neonplay_cache
    ports:
      - "6379:6379"

volumes:
  mongodb_data:
